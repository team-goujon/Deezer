services:
  app:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379
    env_file:
      - .env
  redis:
    image: redis:latest
    command: redis-server --requirepass ${REDIS_PASSWORD}